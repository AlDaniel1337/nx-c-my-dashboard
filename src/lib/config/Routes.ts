interface Rutes {
  counter: string;
  pokemons: string;
  pokemon: string;
}

export const ROUTES: Rutes = {
  counter: '/dashboard/counter',
  pokemons: '/dashboard/pokemons',
  pokemon: '/dashboard/pokemons/pokemon',
};